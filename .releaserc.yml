branches:
  - name: main
  - name: beta
    prerelease: true

plugins:
  - - semantic-release-gitmoji
    - releaseRules:
        major:
          - "💥" # Breaking changes
        minor:
          - "✨" # New feature
          - "🎨" # Improve structure/format
          - "♿" # Accessibility
        patch:
          - "🐛" # Bug fix
          - "🩹" # Simple fix
          - "🔒" # Security fix
          - "⚡" # Performance
          - "💄" # UI/style
          - "♻️" # Refactor
          - "🔧" # Configuration
          - "📝" # Documentation
          - "🏗️" # Architecture
          - "✅" # Tests
          - "🔥" # Remove code/files
          - "🚚" # Move/rename
          - "📦" # Dependencies
          - "🌐" # i18n
          - "🍱" # Assets
          - "🚀" # Deploy
          - "🔖" # Release/version tag

  - - "@semantic-release/changelog"
    - changelogFile: "CHANGELOG.md"

  # Bump de version dans le package.json + configuration du .npmrc (auth NPM_TOKEN),
  # mais SANS publier : la publication réelle est faite par `pnpm publish` ci-dessous.
  - - "@semantic-release/npm"
    - npmPublish: false

  # Publication via pnpm : résout les dépendances `workspace:*` en version concrète
  # UNIQUEMENT dans le tarball publié, sans modifier le package.json du dépôt
  # (le dépôt de dev conserve donc `workspace:*` pour le linking local).
  - - "@semantic-release/exec"
    - publishCmd: "pnpm publish --no-git-checks --access public --tag ${nextRelease.channel || 'latest'}"

  - - "@semantic-release/git"
    - message: "🔖 release v${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      assets:
        - "CHANGELOG.md"
        - "package.json"

  - - "@semantic-release/github"
    - assets:
        - path: CHANGELOG.md
          label: CHANGELOG.md
